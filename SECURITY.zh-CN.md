# 安全政策（Security Policy）

> 本文件是 [SECURITY.md](SECURITY.md)（英文版）的中文翻译。英文版为权威版本。

## 支持的版本

以下版本会收到安全更新：

| 版本 | 支持状态 |
|------|---------|
| v0.x（当前开发版） | ✅ 积极支持 |
| 更早版本 | ❌ 不支持 |

> 本项目尚未发布正式版本（v1.0.0 之前），所有 0.x 版本按"当前开发版"处理。

## 报告漏洞

**不要**在 GitHub Issues、讨论区或任何公开渠道披露安全漏洞。

请通过以下方式私下报告：

1. **邮箱**：发送至 `security@example.com`（请替换为项目实际的安全邮箱）
   - 主题格式：`[featcache-security] <简要描述>`
   - 建议使用 PGP 加密（密钥指纹见项目维护者主页）

2. **GitHub 私有漏洞报告**（推荐）：
   - 在仓库的 **Security → Report a vulnerability** 页面创建私有报告
   - GitHub 私有漏洞报告不会公开可见

### 报告内容建议

- 漏洞描述与影响
- 复现步骤（最小示例优先）
- 受影响版本
- 可能的修复建议（可选）

## 漏洞处理流程

```
T-0   : 收到报告，确认有效性（24 小时内回复）
T+3d  : 提供初步评估（严重程度、影响范围）
T+7d  : 发布修复（如适用）或缓解方案
T+14d : 公开披露（补丁发布后），附 CVE 编号（如申请）
```

时间线可能因漏洞严重程度和修复复杂度调整。

### 严重程度分级

| 级别 | 定义 | 响应时间 |
|------|------|---------|
| Critical | 远程代码执行、数据泄露 | 24h |
| High | 越权访问、数据篡改 | 3d |
| Medium | 信息泄露、DoS | 7d |
| Low | 其他 | 14d |

## 安全最佳实践

### 部署建议

- **共享内存权限**：`/dev/shm/<segment>` 默认 0600，确保仅同用户进程可访问
- **UDS 权限**：文件系统 UDS 默认 `chmod 0777`，生产环境应收紧（如 0755 或按用户隔离）
- **同用户部署**：Loader 与推理进程应以同一系统用户运行
- **多租户环境**：不要在不同租户共享同一个 featcache 实例

### 开发要求

- 新增代码必须通过 `gosec` 安全检查（CI 强制）
- 依赖漏洞由 Dependabot + `govulncheck` 持续监控
- 共享内存边界检查：所有越界读取必须返回错误而非 panic
- 禁止记录敏感数据（Embedding 内容等）到日志

### 已知风险与缓解

| 风险 | 缓解 |
|------|------|
| 共享内存段名称冲突 | `O_EXCL` 创建 + 唯一段名 |
| 段残留（进程崩溃） | 显式 `Destroy()` 清理；运维脚本兜底 |
| hash 种子跨进程不一致 | 见 [ADR-6](docs/design/ADRs.md)，修复中 |
| UDS 抽象命名空间暴露 | 仅限同用户命名空间；敏感环境可限制 |

## 依赖安全

- Dependabot 每周检查依赖更新
- 发布前运行 `govulncheck ./...` 验证无已知漏洞
- 仅允许经过审查的依赖（当前仅 `golang.org/x/sys`）

## 安全更新

安全修复按以下规则发布：

- **Critical/High**：尽快独立发布补丁版本
- **Medium/Low**：随下一个常规版本发布
- 所有安全修复在 [CHANGELOG.md](CHANGELOG.md) 中标注 `[SECURITY]` 前缀
